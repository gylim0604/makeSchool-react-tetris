import {
    MOVE_RIGHT, MOVE_LEFT, MOVE_DOWN, ROTATE,
    PAUSE, RESUME, RESTART, GAME_OVER
} from '.../actions';

const gameReducer = (state = {}, action) =>{
    switch(action.type){
        case ROTATE:
            break;
    }
}